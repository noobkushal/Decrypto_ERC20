/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Decrypto_ERC20', // Matches your repo name
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
